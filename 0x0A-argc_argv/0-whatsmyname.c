#include <stdio.h>
#include "main.h"
/**
 * main - a function that prints program name
 * @argc: arguments number
 * @argv: array
 * Return: Always 0
 */
int main(int argc _attribute_((unused)), char *argv[])
{
		printf("%s\n", *argv);
		return (0);
{
